<% if user_signed_in? %>
<% if current_user.email == 'srizkallah@comcast.net' or current_user.email == 'aheine@olin.edu' %>

	<div class="heading"><h1>Plugged In Contacts</h1><br>
	<h2><%= link_to "Students", students_path %></h2></div>

	<div id="student_list">


	<table class = "pretty">
		<tr><th>First Name: </th><th>Last Name: </th><th>E-mail: </th><th>Home: </th><th>Do: </th></tr>
	<% @students.each do |student| %>

	<td> <%= student.name %></td>
		<td> <%= student.last_name %></td>
		<td> <%= student.email %></td>
		<td> <%= student.home_phone %></td>
		<td><%= link_to 'Show', student %>      
		      <%= link_to 'Edit', edit_student_path(student) %>
		      <%= link_to 'Destroy', student, :confirm => 'Are you sure?', :method => :delete %></td></tr>

	<% end %>
</table>

	
	</div>
	<br />

	<%= link_to 'New Student', new_student_path %>

	
	<div id="user_nav">
	
</div>
<% else %>
	<div class = 'error'>
	<p> This page does not exist </p>
	</div>
<% end %>
<% else %>
	<div class = 'error'>
	<p> This page does not exist </p>
	</div>
<% end %>
