<% if user_signed_in? %>
<% if current_user.email == 'srizkallah@comcast.net' or current_user.email == 'aheine@olin.edu' %>

	<div class="heading"><h1>Plugged In Contacts</h1><br>
	<h2>Results</h2></div>

	<div id="result_list">

	<% @results.each do |result| %>
	<%= form_tag :prospectives_search do %>

	<table class = "pretty">
		<tr><th>First Name: </th><td> <%= result.first %></td><td><%= text_field_tag "first", "" %></td></tr>
		<tr><th>Last Name: </th><td> <%= result.last %></td><td><%= text_field_tag "last", "" %></td></tr>
		<tr><th>Address: </th><td> <%= result.street1 %></td><td><%= text_field_tag "address1", "" %></td></tr>
		<tr><th>E-mail: </th><td> <%= result.email %></td><td><%= text_field_tag "email", "" %></td></tr>
		<tr><th>Home: </th><td> <%= result.home_phone %></td><td><%= text_field_tag "home_phone", "" %></td></tr>
		<tr><th>Cell: </th><td> <%= result.mobile_phone %></td><td><%= text_field_tag "mobile_phone", "" %></td></tr>
		<tr><th>Date of Birth: </th><td> <%= result.dob %></td><td><%= text_field_tag "dob", "" %></td></tr>
		<tr><th>Instrument:</th><td> <%= result.instrument %></td><td><%= text_field_tag "instrument", "" %></td></tr>


		<tr><td><%= link_to 'Show', result %>      
		      <%= link_to 'Edit', edit_prospective_path(result) %>
		      <%= link_to 'Destroy', result, :confirm => 'Are you sure?', :method => :delete %></td><td></td><td>
		<% [ 'And', 'or' ].each do |andor| %>
			<%= radio_button_tag 'andor', andor, @andor == andor %>
			<%= andor.humanize %>
		<% end %>
		<%= submit_tag "Go" %></td></tr>	</table>
	<% end %>
	<% end %>


	
	</div>
	<br />

	<%= link_to 'New Prospective', new_prospective_path %>
	<p> Phone numbers not searchable </p>
	
	<div id="user_nav">
	<% if user_signed_in? %>
		Signed in as <%= current_user.email %>.  Not You?
		<%= link_to "Sign out", destroy_user_session_path %>
	<% else %>
		<%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>.
	<% end %>
</div>
<% else %>
	<div class = 'error'>
	<p> This page does not exist </p>
	</div>
<% end %>
<% else %>
	<div class = 'error'>
	<p> This page does not exist </p>
	</div>
<% end %>
